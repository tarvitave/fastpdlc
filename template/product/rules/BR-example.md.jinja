---
id: BR-example
title: Example business rule
statement: State one enforceable rule for {{ project_name }} here.
applies_to: [TERM-example]
---
`applies_to` must resolve to a real term id — FastPDLC fails the build if it points
at nothing, so your rules can never reference a term that no longer exists.
